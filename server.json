{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.YawLabs/aws-mcp",
  "description": "AWS MCP server — call any AWS API from AI assistants, with first-class SSO re-login",
  "title": "Yaw Labs AWS MCP",
  "repository": {
    "url": "https://github.com/YawLabs/aws-mcp",
    "source": "github",
    "id": "1217190886"
  },
  "websiteUrl": "https://yaw.sh/mcp-servers/aws-mcp/",
  "version": "2.3.4",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@yawlabs/aws-mcp",
      "version": "2.3.4",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "AWS_PROFILE",
          "description": "AWS profile used when a tool call omits `profile`. Any profile your aws CLI can use (SSO, credential_process, role chaining, static keys). Unset: AWS_DEFAULT_PROFILE, then `default`.",
          "isRequired": false,
          "format": "string",
          "placeholder": "my-sso-profile"
        },
        {
          "name": "AWS_REGION",
          "description": "Region used when a tool call omits `region`. Unset: AWS_DEFAULT_REGION, then us-east-1 (a profile's own region setting is not read).",
          "isRequired": false,
          "format": "string",
          "placeholder": "us-west-2"
        }
      ]
    }
  ]
}
